# Phase 7 對抗性驗證結果 — audit bare Unicode 內容掃描接線

A1 CONFIRMED — `git diff --unified=100 HEAD -- cmd/apm/audit.go` 顯示 `--content` 只在 lockfile parse 後插入 early-return，原 bare `VerifyDeployedState`、violation 輸出與成功計數區塊逐行未改；親跑 `go test ./cmd/apm/... -run Audit -count=1` 得 `ok github.com/apm-go/apm/cmd/apm`，且 Audit test list 含 `TestAudit_DeployedFileMismatch`、`TestAuditContent_BareUnaffected`（cmd/apm/audit.go:65, cmd/apm/audit.go:69, cmd/apm/audit_content_test.go:168）。
A2 CONFIRMED — 路徑收集先迭代每個 dependency 的 `DeployedFiles`，再加入 `LocalDeployedFiles`；對抗測試同時放入 dependency critical 與 local warning，兩個路徑都出現在 stderr（cmd/apm/audit_content.go:28, cmd/apm/audit_content.go:31, cmd/apm/audit_content_test.go:119）。
A3 CONFIRMED — CLI 層只對收集到的路徑呼叫共用 `security.ScanFile`，`cmd/apm/audit_content.go` 沒有 suspicious-range 表或 Unicode 分類重實作（cmd/apm/audit_content.go:48, cmd/apm/audit_content.go:50, internal/security/scanner.go:226）。
A4 CONFIRMED — 親建三個含對應 `apm.lock.yaml` 的部署 fixture 並執行新編譯的 `apm-go audit --content`：ASCII clean → exit 0 / `no hidden characters`；U+202E/U+202C bidi override → exit 1；僅 U+200B zero-width space → exit 2；三個 exit code 與 `withExitCode` 分支一致（cmd/apm/audit_content.go:53, cmd/apm/audit_content.go:64, cmd/apm/audit_content.go:68, cmd/apm/exitcode.go:20）。
A5 CONFIRMED — critical fixture 實跑 stderr 為 `critical: bidi-override U+202E at instructions/critical.md:2:8 ...`（另有 U+202C at `:2:15`），同時包含檔案、line 與 column，不是只有總數（cmd/apm/audit_content.go:59）。
B1 REFUTED — Python 的兩支柱與三分類引用本身可由 `commands/audit.py:807-968,1183-1221`、`install/drift.py:625-714` 證實，但文件宣稱 apm-go 對 `unintegrated`「零覆蓋／完全無偵測能力」不實：Python 將 scratch 有而 project 無定義為 `unintegrated`（install/drift.py:667-676），而 apm-go 對 lockfile 已追蹤但磁碟缺失的檔案明確 append violation（internal/lockfile/audit.go:29-33）；親跑 bare fixture 也得到 exit 1 與 `instructions/missing.md ... observed <missing>`，直接反駁文件結論（.trellis/tasks/07-12-p0-parity-quickwins/research/audit-drift-reconciliation.md:9, .trellis/tasks/07-12-p0-parity-quickwins/research/audit-drift-reconciliation.md:74, .trellis/tasks/07-12-p0-parity-quickwins/research/audit-drift-reconciliation.md:117）。
B2 CONFIRMED — 親跑 `apm-go audit --help`，Long 與 flag help 都明列 `--content` 不執行 install-replay drift，亦不實作 `--ci`/`--policy`/`--external`/`--format`/`-o`/`--strip`（cmd/apm/audit.go:39, cmd/apm/audit.go:44, cmd/apm/audit.go:47, cmd/apm/audit.go:92）。
C1 CONFIRMED — 親跑指定命令 `go test ./cmd/apm/... -run Audit -count=1` 得 exit 0 / `ok github.com/apm-go/apm/cmd/apm 1.589s`；`go test ./cmd/apm/... -list Audit` 同時列出 `TestAuditCmd_HelpDocumentsSemanticDifference`、`TestAudit_DeployedFileMismatch`、4 個新 content tests 與全部 marketplace audit tests（cmd/apm/audit_test.go:14, cmd/apm/security_test.go:95）。
C2 CONFIRMED — `git diff --name-only HEAD` 只有 `.trellis/.../implement.md` 與 `cmd/apm/audit.go`；`git status --short` 的 Phase 7 新檔也只有 research/audit-content 檔，`internal/security` 完全沒有 tracked 或 untracked diff。
C3 CONFIRMED — 對五個既有接線檔及 `internal/security` 執行 scoped `git diff --name-only HEAD` 只輸出 `cmd/apm/audit.go`；`cmd/apm/pack.go`、`cmd/apm/install.go`、`internal/manifest/manifest.go`、`internal/manifest/target.go` 均無 Phase 7 diff。
D1 CONFIRMED — 親跑 `go build ./...`、`go vet ./...`、`go test ./...`，三者 exit code 都是 0；全 23 個列出的 Go package 測試均 `ok`。
D2 CONFIRMED — 親跑目前工作樹 `go test ./cmd/apm/... -cover -count=1` 得 84.4%；另以 `git archive HEAD` 的獨立基線樹親跑得 82.6%，目前覆蓋率亦高於 checklist 記載的 84.1% 門檻。
D3 CONFIRMED — `gofmt -l cmd/apm/audit.go cmd/apm/audit_content.go cmd/apm/audit_content_test.go` 無輸出；逐 byte 檢查三檔皆 `crlf_sequences=0`、`bare_cr=0`，為 LF-only。
D4 REFUTED — 既有 help test 的獨立字面只鎖舊的 bare Python contrast line，新 Phase 7 的 drift/flag 排除語意未被精確斷言（cmd/apm/audit_test.go:25, cmd/apm/audit_test.go:33, cmd/apm/audit_content_test.go:67）；用 Go overlay 把產品文案 `does NOT reproduce Python's default install-replay` 反轉成 `DOES reproduce` 後，親跑 `go test ./cmd/apm/... -run Audit -count=1` 仍 exit 0 / PASS，證明測試無法辨識關鍵文案漂移。

OVERALL: FAIL (12/14 confirmed)
