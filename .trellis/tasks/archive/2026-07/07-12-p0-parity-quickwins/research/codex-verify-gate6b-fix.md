A1 CONFIRMED — 在獨立 temp `apm-go-gate6b-codex-07ef5b219b1b40c49b4d27e376257979` 由 test1 唯讀複本重建 `bin/apm-go.exe` 並 pack（exit 0、112 bundle files）；兩個乾淨 consumer 以相對路徑安裝到 `claude,copilot` 均 exit 0/222 files，路徑清單實跑 `GO_PATH_COUNT=403 PY_PATH_COUNT=403 PATH_DIFF_COUNT=0`，test1 前後 449 檔 hash inventory `TEST1_DIFF_COUNT=0`。
A2 CONFIRMED — 對 A1 的 `.claude/.github/.agents` 逐檔 SHA-256 實跑：`GO_FILE_COUNT=222 PY_FILE_COUNT=222 HASH_DIFF_COUNT=0`。
A3 CONFIRMED — A1 Go 部署樹實跑找到 `NESTED_TWO_LEVEL_SKILL_COUNT=78`，包含 `.agents/skills/engineering/ask-matt/SKILL.md` 等 `skills/<category>/<name>/SKILL.md`，且 A1 路徑/hash 雙 diff 皆為 0。
A4 CONFIRMED — A1 實跑 `PRESERVED_AGENT_MD_COUNT=2`、`PRESERVED_INSTRUCTIONS_MD_COUNT=2`；兩端皆保留 `.claude/agents/accessibility-runtime-tester.agent.md` 與 `.claude/instructions/a11y.instructions.md`，未二次 strip/改到 rules。
A5 CONFIRMED — 自造 CRLF bundle 的 source 為 14 bytes/2 CRLF、SHA `4ad3ef64...`；雙邊相對路徑 install 後皆 12 bytes/0 CRLF/2 LF，Go/Python SHA 均為 `2751a3a2f303ad21752038085e2b8c5f98ecff61a2e4ebbd43506a941725be80`、`BYTE_EQUAL=True`（正規化實作 `internal/localbundle/integrate.go:252`）。
A6 REFUTED — 大小寫 metadata fallback 實跑：bundle 實體檔名 `PLUGIN.JSON/.MCP.JSON/APM.LOCK.YAML` 時，Go exit 0 且 `DEPLOYED_UPPER_APM_LOCK=True`（`.claude/APM.LOCK.YAML`），只有 plugin/mcp 被擋；根因是 fallback 僅以 case-sensitive `relSlash == "apm.lock.yaml"` 過濾 lockfile，後段 case-fold 也只列 plugin/mcp（`internal/localbundle/integrate.go:299`、`internal/localbundle/integrate.go:320`）。
B1 CONFIRMED — 複製 bundle 後竄改巢狀 `skills/deprecated/design-an-interface/SKILL.md` 一個 bit，實跑 exit 1 並輸出 `Hash mismatch ... expected 88193ab17903..., got 5f6ce71d00c2...`；`CLAUDE/GITHUB/AGENTS/LOCK_EXISTS=False`，零部署且無 lockfile。
B2 REFUTED — `../escape.txt` key 實跑確實 exit 1/`Unsafe bundle_files entry`，destination 也有 `EnsureWithinRoot`（`internal/localbundle/integrate.go:229`）；但 NTFS `Junction` source 反例 install exit 0，將 bundle 外的 `outside-secret` 複製到 `.claude/skills/linked/SKILL.md`（source/dest SHA 同為 `169f1d37...`），因 symlink sweep 只檢 `os.ModeSymlink`（`internal/localbundle/verify.go:37`），故 Windows 連結式來源逃逸未被拒絕。
B3 CONFIRMED — fresh consumer 成功安裝後 `APM_YML_EXISTS=False`，lockfile 僅出現 `local_deployed_files`/`local_deployed_file_hashes`（另 schema 的 `dependencies: []`）；既有 apm.yml canary 前後 SHA 同為 `aff33189508c6c05c6f84ce0575574ae3565ca88b6a367ca24a6756a6a30d098`、`UNCHANGED=True`。
B4 CONFIRMED — `git diff --name-only HEAD` 僅列 checklist 指定五檔；針對 `internal/localbundle/verify.go internal/localbundle/detect.go` 的 diff 輸出為空，hash/symlink/unlisted 實作本次未改。
C1 CONFIRMED — 親跑 `go build ./...`、`go vet ./...`、`go test ./... -count=1` 均 exit 0；test 輸出 23 個 package 全 `ok`（cmd/apm 22.055s、internal/localbundle 2.106s）。
C2 REFUTED — 舊 transform 斷言已更新（如 claude skill 改斷言 `.claude/skills/bar/SKILL.md`，`cmd/apm/install_localbundle_test.go:69`），但要求的自動化巢狀-skill regression 未新增：兩個 bundle fixture 都只有一層 `skills/bar/SKILL.md`（`cmd/apm/install_localbundle_test.go:23`、`internal/localbundle/testutil_test.go:38`），`go test ... -list 'Test.*(LocalBundle|Nested)'` 也無任何 Nested test。
C3 CONFIRMED — `git diff --name-only HEAD` 僅 `cmd/apm/install.go`、`cmd/apm/install_localbundle_test.go`、`internal/localbundle/{integrate.go,integrate_test.go,testutil_test.go}`；對 `internal/security internal/deploy cmd/apm/{pack.go,audit.go} internal/manifest/{manifest.go,target.go}` 的 diff 輸出為空。
C4 CONFIRMED — 親跑 `D:/Projects/apm-dev/evals/ab_marketplace_pack.py` 得 `total: 14 passed, 0 failed`；`ab_uninstall.py` 得 `total: 6 passed, 0 failed, 2 documented deviations`，兩腳本以 `tempfile` scratch 執行，未碰 test1。
D1 CONFIRMED — 五個觸碰檔 `gofmt -l` 無輸出且逐檔 `CRBytes=0/LFOnly=True`；`go test ./internal/localbundle -cover -count=1` = 82.4%，`go test ./cmd/apm -cover -count=1` = 84.5%，皆達門檻。
D2 CONFIRMED — deviation 註解完整列出 compile-only instruction staging 與 canvas extension default-drop（`internal/localbundle/integrate.go:175`）；binary oracle 實跑 `codex,opencode`：Python stage 到 `apm_modules/test1-1.0.0/.apm/instructions/a11y.instructions.md`、Go 產生兩個明確 skip diagnostic 且 stage count 0；canvas bundle default-off 雙邊皆 exit 0/0 files/無 extension，描述未誇大或縮小。
OVERALL: FAIL (13/16)
