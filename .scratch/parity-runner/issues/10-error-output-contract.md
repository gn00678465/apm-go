# 10 — Cross-command error/warning output contract (systemic F08)

**What to build:** A single decision, implemented once at the `ux` boundary, for where apm-go's user-facing errors and warnings go and how they are prefixed — so that every command matches the Oracle's observable contract (or a single documented, time-boxed deviation), instead of each command drifting on its own.

**Blocked by:** 02 — runner diff/gate attempt 2 (needs a body-level error comparator; see below).

**Status:** CLOSED — attempt 3 (6de0793). eval-ticket-10-r3 Round 4: PASS (clean-provenance corpus /tmp/p10-evidence at bbf60df satisfied all five closing checks; no remaining findings). `browse-unknown-marketplace`/`list-empty` wording → ticket 14; `search-basic-hit` tree diff (marketplaces.json) → ticket 05.

**Origin:** first real parity run (`/tmp/p5`) + `marketplace browse nonexistent` probe + evaluator ruling `.review/eval-ticket-05.md` §B/§C. Oracle: `core/command_logger.py:81-130` (`_rich_*` helpers → one stdout Console), `commands/_helpers.py:72-93` (`_get_console()`).

## The finding

| | Oracle | apm-go today |
|---|---|---|
| error channel | **stdout** | stderr |
| error prefix | `[x] ` | `Error: ` |
| warning channel/prefix | stdout, `[!] ` | stderr, `!` glyph |
| rich table under NO_COLOR/CI/no-TTY | still rendered (box-drawing, no ANSI) | plain fallback |

This is every marketplace command, `doctor`, and `search` — not one command. eval-plan §8.1 forbids normalising channel/severity away; §8.2 #8 lists output channel as an assertion; taxonomy F08 names stderr.

## Acceptance criteria

### Decision (first, recorded in this ticket file before code)
- [x] Choose ONE: (A) align apm-go to the Oracle — errors/warnings to stdout with `[x] `/`[!] ` via `ux`, rich renderers (tables/boxes) always on; or (B) keep apm-go's current contract as a product decision with a dated waiver and an owner. Write the choice, reason, and date here. Default recommendation from the evaluator is (A) as the end state.

**Decision: (A), 2026-08-23.** Confirmed with fresh evidence: `NO_COLOR=1 CI=1 apm-go marketplace browse nonexistent` writes `Error: ...` to stderr (exit 1); the pinned Oracle (`uv run --project .../apm apm marketplace browse nonexistent`, same env) writes `[x] Failed to browse marketplace: ...` to **stdout** (exit 1), nothing on stderr. Read `commands/_helpers.py:72-93`: `_get_console()` builds a `rich.Console(stderr=_console_stderr)` where the module-level `_console_stderr` defaults `False` and is only ever flipped by `set_console_stderr(True)` (reserved for a `--json` mode apm-go doesn't have yet) — so every Oracle command's console output, including `_rich_error`/`_rich_warning` (`utils/console.py:160-172`, prefixes from `STATUS_SYMBOLS` at `utils/console.py:37-61`: `"[x]"` error, `"[!]"` warning), lands on stdout by default. `core/command_logger.py:81-130`'s `CommandLogger.error`/`.warning` are thin wrappers over the same `_rich_error`/`_rich_warning`. No oracle code path routes ordinary errors to stderr. No concrete reason found against (A) — apm-go's current stderr/`Error: ` contract is simply cobra's untouched default (`c.PrintErrln(c.ErrPrefix(), err.Error())`, ErrPrefix() == "Error:"), not a deliberate product decision, so there is nothing to preserve. Proceeding with (A).
- [ ] If (B): every affected case gets its OWN `waivers.json` entry (no wildcard), `fields` limited to the channel/prefix difference, and the runner must first prove the error BODY and exit code are equal (next item) — otherwise the waiver is invalid.

### Runner support (needed for either choice)
- [x] Runner gains an `error_body` diff field: for cases with non-zero exit on either side, strip a leading `[x] `/`Error: `/`[!] `/`!` prefix and leading/trailing whitespace from the FIRST non-empty line of (stdout ∪ stderr) on each side and compare. A channel/prefix-only difference leaves `error_body` equal while `stdout`/`stderr` differ — so a waiver on `stdout`/`stderr` can never hide a wording or exit-code drift. Implemented in `tools/parity/diff.go` (`errorBody`/`stripErrorBodyPrefix`, wired into `diffCase`), covered by `TestDiffCase_ErrorBody_*` in `diff_test.go`.

### Implementation (if A)
- [x] `ux.Error`/`ux.Warn` (and Cobra's root error handler output) write to stdout with `[x] `/`[!] ` prefixes when mirroring the Oracle; one switch in `ux`, no per-command edits. Implemented as `internal/ux/printer.go`'s `errWriter` (redirects a caller-supplied `os.Stderr` to `os.Stdout`, every existing call site unchanged) + `oracleLine` (literal `"[x] "`/`"[!] "` prefix). `cmd/apm-go/main.go`'s root command sets `SilenceErrors: true` and prints the final error itself via `ux.Error` instead of cobra's default `Error:`/stderr.
- [x] `ux.Table`/`ux.Section`/`ux.Box` render box-drawing without ANSI under NO_COLOR/CI/no-TTY instead of falling back to plain lines; `IsRich()` stays for prompt/spinner decisions only. Done in attempt 2: `doctor.go`'s `renderDoctorTable` and `search.go`'s result renderer no longer gate on `ux.IsRich()`; `ux.Table`/`Section`/`Box` themselves already always rendered box-drawing (lipgloss downsamples color only, never the border characters) — only the two commands' own plain-fallback branches needed removing.
- [x] Every existing `cmd/apm-go` test that asserted stderr/`Error:` is updated in the same commit; `go test ./cmd/apm-go/` green.
- [x] Runner cases `browse-unknown-marketplace`, `list-empty` added (per direct instruction, narrower than the ticket's original list): both now show a clean `exit_code`/`stderr` diff (the channel/prefix bug this ticket exists to fix) and only the pre-existing, out-of-scope message-wording + ticket-12 global-config-tree gap remains, field-precisely waived in `waivers.json`. `doctor-git-missing` (ticket 08's fixture doesn't exist yet) and the `search-*` cases (ticket 05's territory, `search.go` explicitly not touched here) are **not** in this commit. Full-corpus run (`tools/parity/cases`, real pinned Oracle) before/after this change: 27/30 → 25/30 unwaived, zero regressions (`pack-refuse-agent-plugin`/`pack-refuse-apm`'s existing waivers updated in the same commit: `stderr` no longer differs and drops out, `error_body` added — same documented exporter-not-implemented gap, not a new one).

### Either way
- [x] `doctor`'s rich/plain split and `search`'s are governed by the same rule; no command chooses its own. Done in attempt 2 (see Attempt-2 items below).
- [x] AGENTS.md "UX layer" paragraph updated in one sentence to state the contract.

## Attempt-2 items (from eval-ticket-10.md)

- [x] **Remove the command-level `IsRich()` plain fallbacks** in `doctor` and `search` — decision (A) means the table path is unconditional (upstream `_get_console()` never returns None in a normal install). Keep `IsRich()` for prompts/spinners only. Both commands' existing tests that asserted `[+] name: detail` plain lines are rewritten to assert the table (box-drawing, no ANSI) under `NO_COLOR=1 CI=1` no-TTY. Also caught and fixed a channel bug this exposed: `doctor.go`'s `renderDoctorTable` was still writing straight to `os.Stderr` (it never routed through `ux.Error`/`ux.Warn`'s `errWriter`), while the pinned Oracle's own Console defaults to stdout for ANY output, not just Warn/Error — moved `w` to `os.Stdout` in the same commit, with `doctor_test.go` gaining a `captureStdout` helper alongside the existing `captureStderr`.
- [x] **`error_body` table-driven tests**: `TestStripErrorBodyPrefix`/`TestErrorBody_TableDriven` in `tools/parity/diff_test.go` cover `[x] `, `[!] `, `Error: `, bare `!` glyph, leading/trailing whitespace, blank lines before the body, body-only-on-stdout, body-only-on-stderr, and both-empty.
- [x] **browse/list wording**: `browse-unknown-marketplace` and `list-empty` show wording diffs (the ticket's own draft said `error_body`; in practice `list-empty` never errors so its gap is a plain `stdout` diff — same intent). NOT waived: `waivers.json` now waives only their `tree` field (the ticket-12 global-config paths, still real for `home/.apm/marketplaces.json`); `stdout`/`error_body` were dropped from both entries so the wording gap shows as a genuine open diff. Recorded as the first two entries of `.scratch/parity-runner/issues/14-marketplace-wording.md`, seeded with the exact old/new from `/tmp/p12/diff/<id>.json`.
- [x] Runner cases `doctor-healthy` and `search-basic-hit` show the table on BOTH sides. `path_prepend` (ticket 08's mechanism; ticket 08 has not landed, so added here) is a new `Case.PathPrepend` field in `tools/parity/case.go`, applied in `runner.go`, covered by `TestRunCaseSide_PathPrependShadowsRealBinary`; `doctor-healthy`'s fixture is a deterministic fake `git` under `cases/doctor-healthy/path/`. Full-corpus run (real pinned Oracle) confirms: `doctor-healthy`'s `stdout` diff is exactly box-drawing/centering/word-wrap (the same pre-existing rich-vs-lipgloss style divergence already documented on `search`'s/`browse`'s own tables) plus a `tree` diff for `home/.local/state/gh/device-id` (the Oracle's auth check shells out to `gh auth token`, which touches gh's own local state; apm-go's auth check never invokes `gh`) — both field-precisely waived. `search-basic-hit`'s remaining `stdout` diff is NOT limited to box-drawing padding (the Oracle also prints a `[>] Searching '...' for '...'...` progress line apm-go doesn't, plus `search`'s Info prefix is a pre-existing gap): ticket 05 explicitly reserves "box-drawing/padding only" as the sole acceptable `search` waiver and is still open on this exact point, so `search-basic-hit` is deliberately left with no waiver here rather than pre-empting ticket 05's decision.

Full-corpus run (`tools/parity/cases`, real pinned Oracle, before attempt 2 → after): 25/30 → 26/31 unwaived (31 cases: `doctor-healthy` added). Net change: `browse-unknown-marketplace`/`list-empty` newly unwaived (+2, deliberate per the wording-gap decision above), `doctor-healthy` newly waived clean (+1 case, 0 net to the unwaived count since it starts waived), zero regressions elsewhere.

## Attempt-3 items (from eval-ticket-10-r2.md FAIL; orchestrator root-cause notes 2026-08-24)

Two blockers from the attempt-2 ruling, plus one latent fixture bug the first fix exposes. The orchestrator has verified all root causes below against a live corpus run; a reference implementation exists in `git stash` ("orchestrator-attempt3-reference") — do NOT pop it; implement fresh, it is only a fallback.

- [ ] **Runner: canonicalize `Case.Dir` to absolute in `LoadCases`** (`tools/parity/case.go`). Today a relative `-cases` flag (the normal CLI shape) leaves `Case.Dir` relative; `runner.go:25` joins it into PATH and the subprocess cwd is the sandbox, so the `path_prepend` entry never resolves and the host git answers — eval-ticket-10-r2.md §4's exact finding. `filepath.Abs` at load time (the field's own doc comment already claims absolute). Regression tests: (a) `LoadCases` on a relative casesDir (`t.Chdir`) yields `filepath.IsAbs(Dir)`; (b) end-to-end `runCaseSide` with a case loaded via relative casesDir still sees the fixture git output. Verify (b) FAILS before the fix.
- [ ] **Fixture: `cases/doctor-healthy/path/git` must answer bare `version` too.** GitPython's import-time probe runs `git version` (no dashes) and parses `stdout.split(" ")[2]` (git/cmd.py `version_info`); the fixture's `*)` arm exits 0 silently, so once the path fix makes the fixture actually reachable, the Oracle's whole CLI import crashes (IndexError → ImportError, empty stdout, exit 1). Add `version` to the `--version)` arm. This was invisible in every prior run because the fixture never took effect.
- [ ] **`ux.Info` renders the Oracle's `"[i] "` prefix** via `oracleLine` (same `errWriter` stdout redirect as Warn/Error) — eval-ticket-10-r2.md §5 found Oracle `[i]` vs apm-go's centered ` i ` on search-basic-hit, and it applies to every `CommandLogger.info/progress` line (STATUS_SYMBOLS "info", utils/console.py:37-61). One change in `internal/ux/printer.go`; call sites unchanged. Tests to update: `printer_test.go` (Info moves from the centered-symbol tests to the oracleLine test), `main_test.go` `TestInitCmd_DoesNotSuggestRun` (Info lines now on stdout → `captureStdout`), `marketplace_e2e_test.go` browse expectations (`"[i] Fetching..."`/`"[i] Install a plugin..."`), `mcpinstall_test.go` `"[i] Targets: ..."`, `plugin_init_format_test.go` `TestPluginInit_AgentFormat_ExistingMCPJSON_ListedInOverwriteNotice` (→ `captureStdout`).
- [ ] **New `ux.Running` printer with `"[>] "`** (STATUS_SYMBOLS "running"/"search"), and `search.go` prints the Oracle's progress line `[>] Searching '<marketplace>' for '<query>'...` (\_\_init\_\_.py:1392, `logger.start(..., symbol="search")`) after the registry lookup succeeds and BEFORE the fetch (a fetch failure still shows it). `search_test.go` asserts the progress line and the `[i]`-prefixed Install hint.
- [ ] **`search-basic-hit` stdout waiver — now legitimate.** After the two fixes above, the normalized stdout diff is exclusively rich HEAVY_HEAD vs ux ROUNDED box glyphs, centered vs left-aligned title, per-cell word-wrap vs column widening (verified on a live run; `rewrite_binary_name` covers the apm/apm-go hint token). That is precisely the sole waiver shape ticket 05 reserves. Field-precise `stdout`-only entry in `waivers.json`; the `tree` diff on `home/.apm/marketplaces.json` (Oracle stores a `file://` URL, apm-go a bare path; sandbox paths differ regardless) stays UNWAIVED — ticket 05's open registry-serialization gap.
- [ ] **Evidence**: full-corpus run; `doctor-healthy` must be waived-clean with the fixture actually answering (`git version 2.45.0` in BOTH sides' records, not the host git); `search-basic-hit` shows `stdout` waived, `tree` open. `go test ./...` and `-race` on `cmd/apm-go`, `internal/ux`, `tools/parity` green (the pre-existing Windows-only `TestParseDepString_AbsolutePath` failure excepted).
