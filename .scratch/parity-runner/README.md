# parity-runner — ticket index

Spec: `.review/spec-surface-gaps-and-parity-runner.md`
Evaluator review that shaped the split: `.review/ticket-review.md`

| # | Ticket | Blocked by |
|---|---|---|
| 01 | Runner: isolated execution and raw capture | — |
| 02 | Runner: normalise, diff, classify, waiver gate, self-test | 01 |
| 03 | Runner: Oracle/Target pin preflight | 01 |
| 04 | Shared bundle-format selector resolver (prefactor) | — |
| 05 | `apm-go search QUERY@MARKETPLACE` | 02, 10 |
| 06 | `marketplace validate --check-refs` hidden no-op | 02 |
| 07 | `pack --format` / `--claude-plugin` selector | 02, 04 |
| 08 | Runner evidence backfill: `doctor` | 02, 03 |
| 09 | Runner evidence backfill: `plugin init` + CI gate | 02, 03, 04, 07 |
| 10 | Cross-command error/warning output contract (systemic F08) | 02 |
| 11 | `validate` missing Structure check + help drift (M-06) | 02 |
| 12 | Global `~/.apm/config.json` + `last_version_check` parity (F09) | 02 |
| 13 | `pack` success output + `--help` parity (pre-existing) | 10 |
| 14 | marketplace command wording parity (browse/list first) | 10 |
| 15 | Runner: drop APM_CONFIG_DIR injection; registry root = sandbox HOME (F04) | 12 |
| 16 | dep-parser full Oracle conformance (table-driven; spun out of 11) | — |
| 17 | `pack` flag/feature parity (spun out of 13) | — |
| 18 | `marketplace audit` missing "Run with --verbose for details." hint (spun out of 14 attempt 2) | — |
| 19 | `init`/`plugin init` output-surface gaps: apm.yml YAML cosmetics, success-output stream split, help_semantic parser limitation (spun out of 09) | — |
| 20 | `marketplace package add ./<path>\` writes a broken name/source; local sources are never stat-ed (user-reported) | — |
| 21 | derived-name rejection blames the name, not the source the user typed (spun out of 20's evaluator follow-up) | 20 |
| 22 | `marketplace validate` output: `[*]`/`[+]` glyphs and single quotes (parity backlog) | — |
| 23 | ~~`search` usage-error hints~~ CLOSED INVALID — Oracle's hint names a command path that does not exist; waivers added instead | — |
| 24 | `marketplaces.json`: `file://` URI form + create-when-empty (parity backlog; was 05's tree gap) | — |
| 25 | record the rendering-library residue as field-scoped waivers (parity backlog) | 22, 23, 24 |
| 26 | `marketplace list` missing its "Registered Marketplaces" title (spun out of 25) | — |

Frontier order: {01, 04} → {02, 03} → {06, 07, 08, 10} → {05} → 09.

Status: ALL TICKETS CLOSED (01-15 ✅; eval-ticket-09.md PASS) — released as v0.3.0-beta.3. 20 closed (user-reported bug, not a parity gap: `marketplace package add`/`check` now require a local source to resolve to an existing directory, and reject a broken package name; zero parity-corpus drift, no cases exist for these commands). 21 IMPLEMENTED, ruling pending (evaluator follow-up on 20's diagnostic wording: a derived-name rejection now blames the source the user typed instead of the derived name, and no message doubles a backslash via `%q`; zero behaviour change, zero parity-corpus drift, both re-verified by the orchestrator). Its AC3 fix initially escaped nothing at all and emitted raw newline/BEL/ESC into the diagnostic; the orchestrator restored escaping for non-printables only (`unicode.IsPrint`) while keeping the single backslash. The independent evaluator ruling is NOT yet written -- codex hit its usage cap mid-verification, resets Aug 31 -- so 21 is not closed. Parity backlog now triaged into root causes (2026-08-25): 22 (validate glyphs, 3 cases) IMPLEMENTED, ruling pending -- orchestrator verifying directly (codex over quota until Aug 31): `marketplace validate`'s header now uses `ux.Gear` (`[*]`, single quotes) and per-check pass rows use a new `ux.Check` (`[+]`) instead of `ux.Info`/`ux.Success`; AC1 found the header is actually `logger.start(..., symbol="gear")` (info-channel), not `logger.success` as the ticket assumed -- same `[*]` byte either way, so the fix stands, only the citation changed. Confirmed via a pre/post parity run: exactly the 3 target cases' `stdout`/`error_body` cleared, all other 66 cases' `(fields, waived)` tuples unchanged. 23 closed INVALID (Oracle's `apm marketplace search` hint names a command path that was never registered anywhere but as top-level `apm search` -- `cli.py:224`'s sole registration, confirmed live via `apm marketplace search --help` -> "No such command"; apm-go's `apm-go search ...` wording is the deliberate, already-documented-correct deviation; 3 waivers added instead, commit `9ca56ee`). 24 IMPLEMENTED, ruling pending -- orchestrator verifying directly: `marketplaces.json` now stores a local source as a `file://` URI (commands/marketplace/__init__.py:288's `f"file://{_expand_local_path(raw)}"`, plain string concat, no escaping) and `LoadRegistry` now materialises `{"marketplaces": []}` on a missing file at load time (registry.py:_load's unconditional `_ensure_file()`, not a lookup-miss special case) -- every consumer of a local source's stored URL (fetch, plugin-source resolution, audit's local-root, and every user-facing display site) was updated to accept/normalise both the new `file://` form and the pre-existing bare-path form, indefinitely (no migration end date). Verified via a pre/post parity run: 15 -> 8 of 69 unwaived; exactly 12 cases' tuples changed, all in the expected direction (`validate-checkrefs-off`/`-on` now fully byte-identical; `browse-unknown-marketplace`/`list-empty`/`search-basic-hit`/`search-unknown-marketplace`/`validate-structure-fail` now covered by pre-existing waivers once their `tree` diff cleared; 5 more `search-*` cases keep an unwaived `stdout` Rich-rendering residue for ticket 25; `registry-explicit-config-dir` explicitly confirmed untouched); all other 57 cases zero change. Also fixed an unrelated, pre-existing `go test` regression from ticket 23's waiver commit (`TestRealWaiversJSON_ValidatesAgainstPin`'s `wantIDs` list was never updated for the 3 new waiver ids). 25 IMPLEMENTED, ruling pending -- re-derived the backlog from a genuinely fresh corpus rather than trusting the stale 2026-08-25 triage table (which 22/24 had already partly closed and 23 partly covered): only 7 cases actually remained (`search-zero-results`/`search-last-at-split` wrap, `search-limit-1`/`search-tag-hit`/`search-description-truncation` table, `search-help` Click-vs-Cobra, `registry-explicit-config-dir` table+`APM_CONFIG_DIR`), matching the orchestrator's own independently-supplied list exactly. Added 6 field-scoped waivers (not 7): investigating `registry-explicit-config-dir`'s `stdout` found a genuine, unwaivable bug -- `marketplace list`'s table is missing an equivalent to the Oracle's "Registered Marketplaces" title entirely (not a differently-aligned instance of the same words; `browse`'s own table already gets this right), so per this ticket's own Non-goals ("no textual differences get waived") that field was left unwaived and spun into ticket 26 instead of being swallowed. `registry-explicit-config-dir`'s pre-existing `tree` waiver (ticket 15, already `tree_paths`-scoped) was untouched. Verified via a fresh corpus run: 8 -> **2 of 69 unwaived** (`pack-help` = ticket 17, `registry-explicit-config-dir` = ticket 26; every other case's tuple unchanged except the 6 newly-waived flipping to `waived: true`), and confirmed zero ghost waivers (the runner's own fail-closed validation ran the full corpus successfully end to end). 26 IMPLEMENTED, ruling pending -- fixed `marketplace list`'s table to print the Oracle's "Registered Marketplaces" title (`fmt.Fprintln` before `ux.Table`, matching browse's/search's own already-correct convention). Verifying AC2 directly against the Oracle (not visually) surfaced a SECOND real gap the title fix alone didn't close: `list`'s headers were hardcoded ALL-CAPS, diverging from the Oracle's Title Case column names -- confirmed `list`-specific (not an apm-go-wide choice) by checking doctor's/search's own tables, which already use Title Case correctly -- so fixed in the same change. Only after both fixes did `registry-explicit-config-dir`'s `stdout` residue reduce to exactly the established `doctor-healthy`/`search-basic-hit` table-rendering-class shape (verified word-for-word); its existing (ticket 15) `tree` waiver was extended to also cover `stdout`, `tree_paths` left untouched. Verified via a fresh corpus run: 2 -> **1 of 69 unwaived** (`pack-help` only, confirmed byte-for-byte unchanged, not touched). A tuple-level diff shows exactly 1 change (`registry-explicit-config-dir` unwaived -> waived); all other 68 cases unchanged. 17 (pack flags, the sole cause of pack-help's help_semantic diff) is now the ONLY item left between the corpus and 0/69 -- IN PROGRESS, phase 1 of 5 (user-approved phased plan, split for context/quota budgeting): `-o`/`--output` wired to the previously-hardcoded OutputDir (with a NEW apm-go-only containment hardening -- the Oracle's own `-o` has no root restriction at all -- reusing internal/marketplace/build.EnsureWithinRoot, the same helper/convention as refcheck.go's resolveLocalSourceAgainstRoot), `-t`/`--target` wired to the pre-existing pack.target lockfile-metadata field (Oracle's own auto-detect-when-absent and comma/alias parsing deliberately NOT replicated -- genuine new logic, out of this phase's scope), and packCmd's Long text replaced with the Oracle's own _PACK_HELP docstring verbatim. Fresh corpus: still 1 of 69 (pack-help, expected -- 6 flags remain: --archive/--archive-format/--check-versions/--check-clean/--json/--legacy-skill-paths); confirmed BOTH new flags' help text is byte-identical to the Oracle's live --help output, but the runner's own help_semantic parser still can't detect this for --target specifically -- Click wraps its long description across 6 physical lines and the parser only reads a flag's first physical line, the SAME pre-existing cross-cutting limitation ticket 19 already documented for pack-help. Zero (fields,waived) regression on all 69 cases, verified via pre/post tuple diff. Phases 2-5 (--archive/--archive-format; --legacy-skill-paths; --check-versions/--check-clean; --json) to follow in separate sessions. Other backlog: 16 (dep-parser conformance rows), 18 (audit verbose hint), 19 (init output-surface gaps). CI parity gate (parity.yml) will go green once 17 fully lands; the go-test job went green at 57e5a9f (CI-detection env is now pinned in TestMain).

Implementor protocol: one ticket per fresh context, via `/implement`. Evaluator verifies each ticket against `diff.jsonl`.

Escalation ladder (finalized 2026-08-24): (1) implementor implements, evaluator verifies, orchestrator coordinates/integrates; (2) after 3 implementor FAILs on the same ticket, the orchestrator takes over implementation directly; (3) after 3 further FAILs on the orchestrator's own work, STOP iterating and re-audit the spec/eval documents themselves (three-hypothesis diagnosis: capability / evaluator interpretation / document defect — ticket 11's root cause was an AC phrase with no depth bound).

Scope rule (added 2026-08-24, after ticket 11 ran to 8 attempts): a ticket's PASS/FAIL boundary is its WRITTEN acceptance criteria. A finding that is real but outside them — in particular any pre-existing gap on a shared, effectively unbounded semantic surface (e.g. the dep-string grammar) — is recorded as a new conformance-table row / new ticket, not a blocker on the current one. This is the same convention tickets 05/10/14 already used for wording and tree gaps.
