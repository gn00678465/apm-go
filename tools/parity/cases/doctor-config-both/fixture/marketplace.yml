# Present only to trigger the apm.yml + marketplace.yml mutually-exclusive
# error; its own content is never parsed on either side (LoadAuthoringConfig
# / detect_config_source both check bare existence for the legacy file).
