[
  {
    "id": "version",
    "fields": [
      "stdout"
    ],
    "taxonomy": "negative-control",
    "oracle_commit": "c8d6cdec596e773a84b0839c33c28b6b0a217637",
    "reason": "--version deliberately prints each binary's own name/version string; this case exists to prove the diff pipeline actually detects a real difference (a negative control), not to hide an unexplained product gap. Reserved for this runner case only -- never accept taxonomy negative-control on a product waiver.",
    "owner": "gn00678465@gmail.com",
    "eval_plan_ref": ".review/spec-surface-gaps-and-parity-runner.md stories 29-32"
  },
  {
    "id": "doctor-healthy",
    "fields": [
      "stdout",
      "tree"
    ],
    "taxonomy": "F01",
    "oracle_commit": "c8d6cdec596e773a84b0839c33c28b6b0a217637",
    "reason": "Added by ticket 10 attempt 2 (path_prepend + a fixture git shim so both sides see identical git/network probe output). Ticket 10 decision (A) fixed doctor's table to be unconditional (no IsRich() plain fallback) AND on the right channel (renderDoctorTable's `w` moved from os.Stderr to os.Stdout in the same commit -- the pinned Oracle's own Console defaults to stdout for ANY output, not just Warn/Error): `stderr` and `exit_code` no longer differ at all. What remains on `stdout` is the same pre-existing, already-documented rendering-library difference recorded on search's/browse's own table (marketplace_browse_table.go, search.go: rich's HEAVY_HEAD box style vs ux.Table's ROUNDED style, plus rich's centered title line vs apm-go's left-aligned one, plus rich's per-cell word-wrap vs ux.Table's column-widening) -- box-drawing and padding only, nothing textual. `tree` covers home/.local/state/gh/device-id (and its parent dirs): the Oracle's auth check delegates to AuthResolver, which shells out to `gh auth token` to probe for a token even when none is configured, and `gh` persists a device-id file under $HOME/.local/state/gh the first time it runs regardless of login state; apm-go's auth check never invokes `gh`. Not added to baseline.json because this is the first runner case to exercise the Oracle's auth-check code path -- if a future case shows the same path, promote it to baseline.json instead of duplicating this waiver.",
    "owner": "gn00678465@gmail.com",
    "eval_plan_ref": ".scratch/parity-runner/issues/10-error-output-contract.md",
    "tree_paths": [
      "home/.local",
      "home/.local/state",
      "home/.local/state/gh",
      "home/.local/state/gh/device-id"
    ]
  },
  {
    "id": "doctor-help",
    "fields": [
      "stdout"
    ],
    "taxonomy": "F01",
    "oracle_commit": "c8d6cdec596e773a84b0839c33c28b6b0a217637",
    "reason": "Intentional rendering-library difference: upstream Click/Rich help and target Cobra/ux help use different Usage/Options/Flags ordering and wording. The doctor help semantic surface, flags, description, exit code, and non-TTY contract were compared separately; only stdout presentation is waived.",
    "owner": "gn00678465@gmail.com",
    "eval_plan_ref": ".review/eval-plan.md §8.3 Rich/Clack vs ux/Cobra"
  },
  {
    "id": "pack-refuse-agent-plugin",
    "fields": [
      "exit_code",
      "stdout",
      "error_body",
      "tree"
    ],
    "taxonomy": "F01",
    "oracle_commit": "c8d6cdec596e773a84b0839c33c28b6b0a217637",
    "reason": "apm-go's pack command does not yet implement the Agent Plugins v1 exporter (bundle/formats.py BundleFormat.AGENT_PLUGIN / packer.py's agent-plugin route). `--format agent-plugin` is accepted by the shared 5-choice resolver and refused with exit 2 before any producer/lockfile/output-directory write, while the pinned Oracle's exporter runs to completion (exit 0) and writes a real Agent Plugin bundle (plugin.json, mcp.json, skills/, embedded apm.lock.yaml) under cwd/build/. Intentional deviation per .review/ticket-review.md §D: Claude path parity is closed; the Agent Plugin exporter is a tracked, out-of-scope gap, not silently hidden -- the CLI surface (help, flag parsing, exit code, exact refusal message) matches the Oracle's contract even though the export itself is unimplemented. tree_paths also names the ticket 12 home/.apm/config.json auto-create and the home/.cache/apm/last_version_check cache file the Oracle writes on every invocation -- neither is specific to --format, both are pre-existing global-footprint gaps this waiver must still name exactly per this case's tree diff. `fields` updated by ticket 10: apm-go's refusal now prints on stdout (matching the Oracle's channel), so `stderr` no longer differs and drops out; `error_body` (ticket 10's new body-only comparator) takes its place here purely because the two sides run entirely different code paths at different exit codes (Oracle succeeds and prints a completion line, apm-go refuses and prints an error) -- not a new gap, the same documented exporter-not-implemented difference the rest of this waiver already covers.",
    "owner": "gn00678465@gmail.com",
    "eval_plan_ref": "§8.3 row 1 / §2.2",
    "tree_paths": [
      "cwd/build",
      "cwd/build/pack-parity-fixture-1.0.0",
      "cwd/build/pack-parity-fixture-1.0.0/apm.lock.yaml",
      "cwd/build/pack-parity-fixture-1.0.0/mcp.json",
      "cwd/build/pack-parity-fixture-1.0.0/plugin.json",
      "cwd/build/pack-parity-fixture-1.0.0/skills",
      "cwd/build/pack-parity-fixture-1.0.0/skills/hello",
      "cwd/build/pack-parity-fixture-1.0.0/skills/hello/SKILL.md",
      "home/.apm",
      "home/.apm/config.json",
      "home/.cache",
      "home/.cache/apm",
      "home/.cache/apm/last_version_check"
    ]
  },
  {
    "id": "browse-unknown-marketplace",
    "fields": [
      "tree"
    ],
    "taxonomy": "F01",
    "oracle_commit": "c8d6cdec596e773a84b0839c33c28b6b0a217637",
    "reason": "Ticket 10 attempt 2: this entry no longer covers `stdout`/`error_body` -- per direct instruction those wording diffs (`marketplaceNotRegisteredErr`'s apm-go-specific phrasing vs. the Oracle's \"Failed to browse marketplace: Marketplace '...' is not registered.\") are deliberately left UNWAIVED and tracked as the first entry of ticket 14 (marketplace command wording parity), so this case shows as a genuine open gap in the runner's gate rather than being hidden. Ticket 10 itself already fixed the channel/prefix gap this case originally existed to pin (apm-go's error now lands on stdout with the Oracle's \"[x] \" prefix, so `exit_code` no longer differs). This entry still covers the `tree` field for exactly the paths in tree_paths: the Oracle auto-creates home/.apm/config.json and home/.cache/apm/last_version_check on ANY invocation (ticket 12 scope, now baseline-excluded) plus home/.apm/marketplaces.json, a real product-specific path outside the baseline.",
    "owner": "gn00678465@gmail.com",
    "eval_plan_ref": ".scratch/parity-runner/issues/12-global-config-parity.md",
    "tree_paths": [
      "home/.apm",
      "home/.apm/config.json",
      "home/.apm/marketplaces.json",
      "home/.cache",
      "home/.cache/apm",
      "home/.cache/apm/last_version_check"
    ]
  },
  {
    "id": "list-empty",
    "fields": [
      "tree"
    ],
    "taxonomy": "F01",
    "oracle_commit": "c8d6cdec596e773a84b0839c33c28b6b0a217637",
    "reason": "Ticket 10 attempt 2: this entry no longer covers `stdout` -- per direct instruction the wording diff (apm-go's Info prefix is the bare centered \" i \" vs. the Oracle's \"[i] \", plus apm-go's own shorter hint wording vs. the Oracle's two-line \"Use 'apm marketplace add SOURCE' to register one (OWNER/REPO, HTTPS URL, SSH URL, or local path).\") is deliberately left UNWAIVED and tracked as the second entry of ticket 14 (marketplace command wording parity), so this case shows as a genuine open gap in the runner's gate rather than being hidden. `marketplace list` with none registered was already on the right channel/severity before ticket 10 (both sides print an informational, not an error/warning, line) -- this case remains a regression guard proving ticket 10's Warn/Error-only channel switch left Info untouched. This entry still covers the `tree` field for exactly the paths in tree_paths: the Oracle auto-creates home/.apm/config.json and home/.cache/apm/last_version_check on ANY invocation (ticket 12 scope, now baseline-excluded) plus home/.apm/marketplaces.json, a real product-specific path outside the baseline.",
    "owner": "gn00678465@gmail.com",
    "eval_plan_ref": ".scratch/parity-runner/issues/12-global-config-parity.md",
    "tree_paths": [
      "home/.apm",
      "home/.apm/config.json",
      "home/.apm/marketplaces.json",
      "home/.cache",
      "home/.cache/apm",
      "home/.cache/apm/last_version_check"
    ]
  },
  {
    "id": "pack-refuse-apm",
    "fields": [
      "exit_code",
      "stdout",
      "error_body",
      "tree"
    ],
    "taxonomy": "F01",
    "oracle_commit": "c8d6cdec596e773a84b0839c33c28b6b0a217637",
    "reason": "apm-go's pack command does not yet implement the legacy APM bundle layout exporter (bundle/formats.py BundleFormat.APM / packer.py's apm route). `--format apm` is accepted by the shared 5-choice resolver and refused with exit 2 before any producer/lockfile/output-directory write, while the pinned Oracle's exporter runs to completion (exit 0, \"No deployed files found -- empty bundle created\" since this fixture has no per-target deployed artifacts) and writes a real (if minimal) APM-format bundle under cwd/build/. Intentional deviation per .review/ticket-review.md §D: Claude path parity is closed; the APM exporter is a tracked, out-of-scope gap, not silently hidden -- the CLI surface (help, flag parsing, exit code, exact refusal message) matches the Oracle's contract even though the export itself is unimplemented. tree_paths also names the ticket 12 home/.apm/config.json auto-create and the home/.cache/apm/last_version_check cache file the Oracle writes on every invocation -- neither is specific to --format, both are pre-existing global-footprint gaps this waiver must still name exactly per this case's tree diff. `fields` updated by ticket 10: apm-go's refusal now prints on stdout (matching the Oracle's channel), so `stderr` no longer differs and drops out; `error_body` (ticket 10's new body-only comparator) takes its place here purely because the two sides run entirely different code paths at different exit codes (Oracle succeeds and prints a completion line, apm-go refuses and prints an error) -- not a new gap, the same documented exporter-not-implemented difference the rest of this waiver already covers.",
    "owner": "gn00678465@gmail.com",
    "eval_plan_ref": "§8.3 row 1 / §2.2",
    "tree_paths": [
      "cwd/build",
      "cwd/build/pack-parity-fixture-1.0.0",
      "cwd/build/pack-parity-fixture-1.0.0/apm.lock.yaml",
      "home/.apm",
      "home/.apm/config.json",
      "home/.cache",
      "home/.cache/apm",
      "home/.cache/apm/last_version_check"
    ]
  }
]
