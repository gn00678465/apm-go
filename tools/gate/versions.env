# Pinned dev-tool versions for tools/gate.sh. Each tool runs via
# `go run <module>@<version>` so the rerun uses the same checker; the Go
# toolchain itself is pinned by go.mod's `go` directive.
STATICCHECK_VERSION=2026.2.1
GOVULNCHECK_VERSION=v1.7.0
